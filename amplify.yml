version: 1

frontend:
  phases:
    preBuild:
      commands:
      
    build:
      commands:
  artifacts:
    baseDirectory: 
    files:

  cache:
    paths:
      
backend:
  phases:
    build:
      commands:
        - yarn install
        
    postBuild:
      commands:
        - yarn build
  artifacts:
    baseDirectory: server/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

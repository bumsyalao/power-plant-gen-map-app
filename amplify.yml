version: 1
   
frontend:
  phases:
    build:
      commands:
        - yarn install
        
    postBuild:
      commands:
        - yarn build
  artifacts:
    baseDirectory: server/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

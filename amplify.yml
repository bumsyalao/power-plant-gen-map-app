version: 1

backend:
  phases:
    build:
      commands:
        - yarn install
        
    postBuild:
      commands:
        - yarn build
  artifacts:
    baseDirectory: server/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
